import React from 'react';
import {Layout} from "../common/layout";

export const Route2 = (props) => {
    return (
        <Layout props={props}>
            <div className="route-2-53d">
                route 2
            </div>
        </Layout>
    )
}
