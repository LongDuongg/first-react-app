import React from 'react';
import { Layout } from '../common/layout';
import './subject.scss';

export const Subject = (props) => {
    return (
        <Layout props={props}>
            <div className='subject-1j2'>
                Môn học
            </div>
        </Layout>
    )
}
